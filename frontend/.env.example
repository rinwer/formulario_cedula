VITE_API_URL=http://localhost:8000

# Credenciales publicas de Supabase (Settings > API en el dashboard).
# Usar la anon key (NO la service_role): esta clave es publica por diseno,
# solo sirve para iniciar sesion (auth) y queda sujeta a RLS.
VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=TU_ANON_KEY
